specVersion: "v1beta"
serviceId: "project-sensor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-sensor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
